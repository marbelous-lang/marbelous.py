}0
^0 .. # extract the first bit from the input
=0 &2 # drop "1" or "i" depending on that bit
&1 \/
}0 .. # extract the second bit from the input
^1 .. # drop "-" or not depending on that bit
=0 &0 2D 31 69 # "-","1","i"
\/ \/ &0 &1 &2